sonobuoy-config:
  driver: Job
  plugin-name: azure-arc-flux
  result-format: junit
spec:
  image: joinnisacr.azurecr.io/microsoft-flux-conformance:0.0.4
  imagePullPolicy: Always
  name: plugin
  resources: {}
  volumes:
  - name: results
    emptyDir: {}
  volumeMounts:
    - mountPath: /tmp/results
      name: results