sonobuoy-config:
  driver: Job
  plugin-name: azure-arc-flux
  result-format: junit
spec:
  image: joinnisacr.azurecr.io/conformance:latest
  imagePullPolicy: Always
  name: plugin
  resources: {}
  volumes:
  - name: results
    emptyDir: {}
  volumeMounts:
    - mountPath: /tmp/results
      name: results